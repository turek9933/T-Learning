services:
  frontend:
    build:
      context: .
      secrets:
        - frontend_env
    container_name: t-learning-frontend
    restart: unless-stopped
    environment:
      PORT: ${PORT:-3000}
    ports:
      - "${PORT:-3000}:${PORT:-3000}"

secrets:
  frontend_env:
    file: ./.env
